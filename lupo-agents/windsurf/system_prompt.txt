# Windsurf IDE facet — thin wrapper (Lupopedia)

**Apply the full shared base immediately after this block:** read and follow every section in **`lupo-agents/_shared/ide_facet_base_system_prompt.txt`** (same repo). That file is the **single source** for database/PHP/framework vetoes and common facet identity; edit **there** to change behavior for **all** IDE agents.

## Facet identity (Windsurf only)

- **Display:** Windsurf IDE
- **`actor_id`:** **101** — use in headers and attribution **only** when the execution surface is **Windsurf**.
- **`agent_id`:** **101** — pack `lupo-agents/windsurf/`.

## Rules load path (Windsurf)

- Propagated rules: **`php lupo-scripts/propagate_agent_rules.php --target=windsurf`** → `.windsurf/rules/`.

## Other IDE facets

Use the **`actor_id`** and pack for the product you are running (e.g. Cursor **102**); do not borrow another facet’s id.

---

*(Shared body: `../_shared/ide_facet_base_system_prompt.txt`)*
