---
lupopedia.headers:
  header_format_version: "4.1.8"
  path_from_lupopedia_root: actors/windsurf/prompts/20260309_v4.0.67_release_and_4.0.68_init.md
  web_path: https://www.lupopedia.com/lupopedia/actors/windsurf/prompts/20260309_v4.0.67_release_and_4.0.68_init.md
  status: null
  when_updated: '20260513033046'
  trust_tier: null
  questions_toon: null
  memory_toon: null
  atoms_toon: null
  transcript_jsonl: null
  artifact_type: directive
  artifact_kind: release_task
  channel_key: null
  federation_node_id: null
  thread_key: null
  lupopedia.schema: directive
  prd_cluster: null
  title: null
  summary: null
---

# WINDSURF DIRECTIVE — RELEASE v4.0.67 AND INITIALIZE v4.0.68

**To:** Windsurf IDE Agent (`actor_id: 1001`)  
**From:** Captain Wolfie  
**Current Release Target:** 4.0.67  
**Next Development Target:** 4.0.68  
**Priority:** High  
**Subject:** Git add + commit + push v4.0.67, then initialize v4.0.68 and create the first task in Channel 42

## Mission

You are responsible for completing the release handoff for **v4.0.67** and immediately opening the next development cycle for **v4.0.68**.

This directive has three parts, in this exact order:

1. stage and push the completed **v4.0.67** work to GitHub
2. initialize all canonical version references from **4.0.67** to **4.0.68**
3. create a new task in **Channel 42**, thread **4.0.68**, for finishing the channels web interface

Do not mix the release commit and the version-bump/init work into one indistinct action.  
Keep the release boundary clean and auditable.

---

# TASK 1 — Release v4.0.67 to GitHub

## Required actions

Run the full git release flow for the completed **4.0.67** state:

```bash
git add .
git commit -m "Release v4.0.67 hardening and verification"
git push
```

## Requirements

* make sure the repo is in a clean, coherent state before commit
* do not discard or overwrite any work from Cursor, Antigravity, Gemini-cli, or Windsurf
* preserve all 4.0.67 changelog and report work already completed
* if there are uncommitted files unrelated to 4.0.67, explicitly identify them before committing

## Verification

After push, report:

* commit hash
* branch pushed
* whether working tree is clean

---

# TASK 2 — Initialize version 4.0.68

Immediately after the 4.0.67 push, begin the next cycle by updating canonical version references from:

```text
4.0.67 → 4.0.68
```

## Required scope

Update all canonical version files and atoms, including at minimum:

* `config/config/global_atoms.yaml` 
* `config/global_atoms.yaml` 
* `config/config/GLOBAL_IMPORTANT_ATOMS.yaml` 
* `config/GLOBAL_IMPORTANT_ATOMS.yaml` 
* `includes/functions/load_atoms.php` 
* `bin/lupo.php` 

Also inspect for other canonical `version.*` or equivalent files that define the system version and update them if they are part of the official version surface.

## Rules

* update only canonical version references for cycle initialization
* do not rewrite the 4.0.67 changelog entry
* do not fabricate new 4.0.68 feature claims
* keep 4.0.68 initialization minimal and clean

## Verification

Report every file changed for the 4.0.68 bump and the exact old/new version replacement.

---

# TASK 3 — Create first 4.0.68 task in Channel 42

Create a task in **Channel 42**, thread **4.0.68**, stating that the next priority is to finish the **channels web interface**.

## Task content requirements

The task must clearly state:

* the target is the **web interface for channels**
* the relevant code is in the **installed** subdirectory
* the interface is accessed at:

```text
http://domainname.com/<lupopedia-sub-folder>/channels/
```

* this is a **4.0.68 priority task**
* the goal is to finish and harden that web interface

## Suggested task wording

Use something close to this:

> **4.0.68 Priority Task:** Finish the web interface for Channels.
> The relevant implementation is in the installed subdirectory and is accessed from:
> `http://domainname.com/<lupopedia-sub-folder>/channels/` 
> This version cycle should review, complete, and harden the channels web UI so it is fully operational and aligned with current doctrine/schema/runtime expectations.

## Rules

* create this as a task in **Channel 42**
* place it in thread **4.0.68**
* do not bury it inside unrelated release notes
* make it explicit that this is the first tracked priority for the new cycle

---

# Completion Output Required

When finished, report back with:

## 1. Git release result

* commit hash
* commit message
* branch pushed
* push success status

## 2. Version initialization result

* exact files updated from 4.0.67 to 4.0.68
* any canonical version files found beyond the expected set

## 3. Channel 42 task result

* confirmation that the 4.0.68 thread/task was created
* exact wording used for the task

## 4. Final repo state

* whether the repo is clean after the 4.0.68 initialization work
* whether a second commit was made for the version bump/init, and if so, its hash/message

---

# Preferred commit structure

Use two clean commits if appropriate:

## Commit A

Release completion for **4.0.67**

Example:

```text
Release v4.0.67 hardening and verification
```

## Commit B

Initialize **4.0.68**

Example:

```text
Initialize v4.0.68 version atoms and next-cycle task
```

This separation is preferred because it preserves a clean release boundary.

---

# Final instruction

Preserve 4.0.67 exactly as released.
Start 4.0.68 cleanly.
Do not overwrite other agents' completed work.
Keep the release and initialization steps auditable.

**End of directive.**
