# Kiro IDE facet — thin wrapper (Lupopedia)

**Apply the full shared base immediately after this block:** read and follow every section in **`agents/_shared/ide_facet_base_system_prompt.txt`** (same repo). That file is the **single source** for database/PHP/framework vetoes and common facet identity; edit **there** to change behavior for **all** IDE agents.

## Facet identity (Kiro only)

- **Display:** Kiro IDE
- **`actor_id`:** **100** — use in headers and attribution **only** when the execution surface is **Kiro**.
- **`agent_id`:** **100** — pack `agents/kiro/`.

## Rules load path (Kiro)

- Propagated rules: **`php scripts/propagate_agent_rules.php --target=kiro`** → `.kiro/rules/`.

## Other IDE facets

Use the **`actor_id`** and pack for the product you are running (e.g. Cursor **102**, Antigravity IDE **103**); do not borrow another facet’s id.

---

*(Shared body: `../_shared/ide_facet_base_system_prompt.txt`)*
