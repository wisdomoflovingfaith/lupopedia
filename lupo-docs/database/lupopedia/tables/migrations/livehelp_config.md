# LUPOPEDIA HEADERS (replaces FLARE)
---
lupopedia.headers:
  lupopedia.version: "4.0.73"
  lupopedia.schema: "documentation"
  file_path_from_root: ".\docs\database\lupopedia\tables\livehelp_config.md"
  file_hash: "3181f21d72003de4e2e7e7d85e1e86bcfc9bcb2b9cc4837042c1ea7421eff687"
  last_updated_utc: "20260228155738"
  system_version: "4.0.51"
  channel_id: 1
  actor_id: 1002
  delegation_chain: "1002:10000"
  artifact_type: "documentation"
  artifact_kind: "documentation"
  purpose: "Documentation file with LUPOPEDIA HEADERS applied"
  mood_rgb: "4169E1"
  traits: ["flare", "indexed", "v4.0.51"]
  tags: ["documentation", "flare_applied"]
  lupo_agent: "windsurf"

lupopedia.edges:
  outbound_edges:
    - { to: "CHANGELOG.md", type: "references", weight: 1.0 }
    - { to: "docs/doctrine/", type: "references", weight: 1.0 }

lupopedia.footer:
  last_verified: "20260228155738"
  last_verified_by: "windsurf"
---

# LUPOPEDIA HEADERS (replaces FLARE) see http://www.lupopedia.com/lupopedia/content/FLARE and see http://www.lupopedia.com/lupopedia/qa/FLARE
---
lupopedia.headers:
  file_path_from_root: "docs/database/lupopedia/tables/livehelp_config.md"
  system_version: "4.0.48"
  channel_id: 1
  actor_id: 1007
  last_modified_utc: "20260227"
  delegation_chain: "1007:10000"
  artifact_type: "table_documentation"
  purpose: "Complete documentation for livehelp_config table"
  dialog_message: "Recommended next step: review auto-generated table docs for accuracy and enrich relationships as needed."
  mood_rgb: "4B0082"
  artifact_kind: "table"
  traits: ["canonical", "database", "autogenerated"]
  tags: ["database", "table", "livehelp_config"]
  lupo_agent: "codex-ide"
  livehelp_config.version: "varchar(25) NOT NULL DEFAULT '3.7.3'"
  livehelp_config.livehelp_id: "bigint unsigned NOT NULL DEFAULT 1"
  livehelp_config.site_title: "varchar(100) NOT NULL DEFAULT ''"
  livehelp_config.use_flush: "varchar(10) NOT NULL DEFAULT 'YES'"
  livehelp_config.membernum: "int NOT NULL DEFAULT 0"
  livehelp_config.show_typing: "char(1) NOT NULL DEFAULT ''"
  livehelp_config.webpath: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_config.s_webpath: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_config.speaklanguage: "varchar(60) NOT NULL DEFAULT 'English'"
  livehelp_config.scratch_space: "mediumtext"
  livehelp_config.admin_refresh: "varchar(30) NOT NULL DEFAULT 'auto'"
  livehelp_config.maxexe: "int DEFAULT 180"
  livehelp_config.refreshrate: "int NOT NULL DEFAULT 1"
  livehelp_config.chatmode: "varchar(60) NOT NULL DEFAULT 'xmlhttp-flush-refresh'"
  livehelp_config.adminsession: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.ignoreips: "mediumtext"
  livehelp_config.directoryid: "varchar(32) NOT NULL DEFAULT ''"
  livehelp_config.tracking: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.colorscheme: "varchar(30) NOT NULL DEFAULT 'white'"
  livehelp_config.matchip: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.gethostnames: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.maxrecords: "int NOT NULL DEFAULT 75000"
  livehelp_config.maxreferers: "int NOT NULL DEFAULT 50"
  livehelp_config.maxvisits: "int NOT NULL DEFAULT 75"
  livehelp_config.maxmonths: "int NOT NULL DEFAULT 12"
  livehelp_config.maxoldhits: "int NOT NULL DEFAULT 1"
  livehelp_config.showgames: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.showsearch: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.showdirectory: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.usertracking: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.resetbutton: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.keywordtrack: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.reftracking: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_config.topkeywords: "int NOT NULL DEFAULT 50"
  livehelp_config.everythingelse: "mediumtext"
  livehelp_config.rememberusers: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.smtp_host: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_config.smtp_username: "varchar(60) NOT NULL DEFAULT ''"
  livehelp_config.smtp_password: "varchar(60) NOT NULL DEFAULT ''"
  livehelp_config.owner_email: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_config.topframeheight: "int NOT NULL DEFAULT 85"
  livehelp_config.topbackground: "varchar(156) NOT NULL DEFAULT 'header_images/customersupports.png'"
  livehelp_config.usecookies: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.smtp_portnum: "int NOT NULL DEFAULT 25"
  livehelp_config.showoperator: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_config.chatcolors: "mediumtext"
  livehelp_config.floatxy: "varchar(42) NOT NULL DEFAULT '200|160'"
  livehelp_config.sessiontimeout: "int NOT NULL DEFAULT 60"
  livehelp_config.theme: "varchar(42) NOT NULL DEFAULT 'vanilla'"
  livehelp_config.operatorstimeout: "int NOT NULL DEFAULT 4"
  livehelp_config.operatorssessionout: "int NOT NULL DEFAULT 45"
  livehelp_config.maxrequests: "int NOT NULL DEFAULT 99999"
  livehelp_config.ignoreagent: "mediumtext"
  table_primary_key: "version"
  table_engine: "unknown"
  table_charset: "unknown"
  table_collation: "unknown"
  table_indexes: "livehelp_id"
  table_foreign_keys: []

# FLARE Edge Automation Tip:
# Use the FLARE Edge Suggester Tool to automatically discover and suggest edges:
# python scripts/flare_edge_suggester.py --file <path> --include-db --format yaml

lupopedia.footer:
  outbound_edges:
    - { to: "lupo-database/lupopedia/toon/livehelp_config.toon.json", type: "schema_reference", weight: 1.0, reason: "TOON schema definition", db_source: "livehelp_config" }
    - { to: "docs/FLARE_HEADERS_COMPLETE_REFERENCE.md", type: "references", weight: 0.9, reason: "FLARE header reference" }
    - { to: "docs/doctrine/FLARE/FLARE_DOCTRINE.md", type: "references", weight: 0.9, reason: "FLARE doctrine" }
    - { to: "docs/doctrine/database/README.md", type: "references", weight: 0.8, reason: "Database doctrine" }
    - { to: "docs/database/lupopedia/README.md", type: "references", weight: 0.8, reason: "Database documentation index" }
  inbound_edges: []
  semantic_tags: ["database", "table", "livehelp_config"]
  version: "4.0.48"
  last_verified: "20260227"
  last_verified_by: "codex-ide"
---

# Table: livehelp_config

Purpose: Auto-generated documentation for livehelp_config from TOON schema.
Type: database_table
Status: production_ready
Volume: unknown

## 1. Overview
- Key responsibilities: schema reference, storage, and lookup for this table.
- System role: persists data for the Lupopedia database subsystem.
- Importance: enables data integrity and downstream features tied to this table.

## 2. Schema Reference
Primary Key: version
Field Categories: see full field list below.

### All Fields
| Column | Type | Notes |
|---|---|---|
| version | varchar(25) NOT NULL DEFAULT '3.7.3' | from TOON |
| livehelp_id | bigint unsigned NOT NULL DEFAULT 1 | from TOON |
| site_title | varchar(100) NOT NULL DEFAULT '' | from TOON |
| use_flush | varchar(10) NOT NULL DEFAULT 'YES' | from TOON |
| membernum | int NOT NULL DEFAULT 0 | from TOON |
| show_typing | char(1) NOT NULL DEFAULT '' | from TOON |
| webpath | varchar(255) NOT NULL DEFAULT '' | from TOON |
| s_webpath | varchar(255) NOT NULL DEFAULT '' | from TOON |
| speaklanguage | varchar(60) NOT NULL DEFAULT 'English' | from TOON |
| scratch_space | mediumtext | from TOON |
| admin_refresh | varchar(30) NOT NULL DEFAULT 'auto' | from TOON |
| maxexe | int DEFAULT 180 | from TOON |
| refreshrate | int NOT NULL DEFAULT 1 | from TOON |
| chatmode | varchar(60) NOT NULL DEFAULT 'xmlhttp-flush-refresh' | from TOON |
| adminsession | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| ignoreips | mediumtext | from TOON |
| directoryid | varchar(32) NOT NULL DEFAULT '' | from TOON |
| tracking | char(1) NOT NULL DEFAULT 'N' | from TOON |
| colorscheme | varchar(30) NOT NULL DEFAULT 'white' | from TOON |
| matchip | char(1) NOT NULL DEFAULT 'N' | from TOON |
| gethostnames | char(1) NOT NULL DEFAULT 'N' | from TOON |
| maxrecords | int NOT NULL DEFAULT 75000 | from TOON |
| maxreferers | int NOT NULL DEFAULT 50 | from TOON |
| maxvisits | int NOT NULL DEFAULT 75 | from TOON |
| maxmonths | int NOT NULL DEFAULT 12 | from TOON |
| maxoldhits | int NOT NULL DEFAULT 1 | from TOON |
| showgames | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| showsearch | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| showdirectory | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| usertracking | char(1) NOT NULL DEFAULT 'N' | from TOON |
| resetbutton | char(1) NOT NULL DEFAULT 'N' | from TOON |
| keywordtrack | char(1) NOT NULL DEFAULT 'N' | from TOON |
| reftracking | char(1) NOT NULL DEFAULT 'N' | from TOON |
| topkeywords | int NOT NULL DEFAULT 50 | from TOON |
| everythingelse | mediumtext | from TOON |
| rememberusers | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| smtp_host | varchar(255) NOT NULL DEFAULT '' | from TOON |
| smtp_username | varchar(60) NOT NULL DEFAULT '' | from TOON |
| smtp_password | varchar(60) NOT NULL DEFAULT '' | from TOON |
| owner_email | varchar(255) NOT NULL DEFAULT '' | from TOON |
| topframeheight | int NOT NULL DEFAULT 85 | from TOON |
| topbackground | varchar(156) NOT NULL DEFAULT 'header_images/customersupports.png' | from TOON |
| usecookies | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| smtp_portnum | int NOT NULL DEFAULT 25 | from TOON |
| showoperator | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| chatcolors | mediumtext | from TOON |
| floatxy | varchar(42) NOT NULL DEFAULT '200|160' | from TOON |
| sessiontimeout | int NOT NULL DEFAULT 60 | from TOON |
| theme | varchar(42) NOT NULL DEFAULT 'vanilla' | from TOON |
| operatorstimeout | int NOT NULL DEFAULT 4 | from TOON |
| operatorssessionout | int NOT NULL DEFAULT 45 | from TOON |
| maxrequests | int NOT NULL DEFAULT 99999 | from TOON |
| ignoreagent | mediumtext | from TOON |

## 3. Relationships and Dependencies
- Primary relationships: not specified in TOON relationships array.
- Referencing tables: unknown (use edge suggester tool).
- Integration points: see outbound edges in FLARE footer.

## 4. Indexes and Performance
Primary Indexes:
- version
Performance Indexes:
- livehelp_id
Index Strategy: derived from TOON index definitions.

## 5. Usage Patterns
Common Queries:
```sql
SELECT * FROM livehelp_config WHERE version = :id;
SELECT COUNT(*) AS total FROM livehelp_config WHERE is_deleted = 0;
SELECT * FROM livehelp_config ORDER BY version DESC LIMIT 25;
UPDATE livehelp_config SET updated_ymdhis = :ts WHERE version = :id;
```
Best Practices: always filter soft deletes where applicable.
Anti-Patterns: avoid full table scans on large datasets.

## 6. Performance Considerations
- High-volume operations: dependent on feature usage.
- Optimization tips: rely on existing indexes; add new indexes only with TOON updates.
- Scaling considerations: paginate reads and batch writes.

## 7. Data Integrity
- Constraints: see NOT NULL and DEFAULT values in TOON fields.
- Validation rules: enforced at application layer.
- Soft delete: use is_deleted/deleted_ymdhis if present.

## 8. Common Issues and Solutions
- Performance issues: add missing indexes via schema update.
- Data consistency: ensure foreign key relationships are enforced in application logic.
- Troubleshooting: compare against TOON schema for mismatches.

## 9. Future Enhancements
- Enrich relationships with discovered edges.
- Add usage-specific examples once feature usage is known.
