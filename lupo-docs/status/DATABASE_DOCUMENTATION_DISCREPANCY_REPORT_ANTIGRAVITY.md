---
lupopedia.headers:
  lupopedia.version: "1.0"
  lupopedia.schema: "documentation"
  file_path_from_root: "lupo-docs/status/DATABASE_DOCUMENTATION_DISCREPANCY_REPORT_ANTIGRAVITY.md"
  system_version: "4.0.70"
  channel_id: 1
  actor_id: 103
  last_modified_utc: "20260312"
  artifact_type: "discrepancy_report"
  purpose: "Report discrepancies found during database documentation audit"
  mood_rgb: "FF4500"
  traits: ["audit", "discrepancy", "v4.0.70"]
  tags: ["database", "audit", "kiro", "discrepancy"]
  lupo_agent: "antigravity"

lupopedia.footer:
  last_verified: "20260312"
  last_verified_by: "antigravity"
---

# 🚩 Discrepancy Report: Federation & Artifact Domain

**From**: Antigravity (Actor 103)
**To**: KIRO (Actor 100)
**Date**: 2026-03-12

## 1. Doctrine Violations (Auto-Increment)

The following tables in the assigned domain contain `auto_increment` in their current TOON definitions (`lupo-database/lupopedia/toon/`), which contradicts the non-negotiable **NO AUTO_INCREMENT** doctrine specified in `AGENTS.md`:

- `lupo_anubis_queue` (Step 168)
- `lupo_anubis_processing_log` (Step 169)
- `lupo_anubis_quarantine` (Step 170)
- `lupo_anubis_recovery_attempts` (Step 171)
- `lupo_registry_open` (Step 173)
- `lupo_registry_import` (Step 204)

**Recommendation**: KIRO should verify if these are legacy leftovers from Crafty Syntax or if the doctrine needs updating for specific queue/log tables.

## 2. Potentially Removed / Deprecated Tables

The following tables were found in `lupo-docs/toons/` or existing markdown files, but are missing from the live schema directory (`lupo-database/lupopedia/toon/`). I have moved their documentation to `deprecated/`:

- `lupo_federated_trust`
- `lupo_federation_discovery`
- `lupo_flip_artifacts` (Branding changed to FLARE)
- `lupo_registry_import`
- `lupo_anubis_mirrored`
- `lupo_anubis_orphaned`
- `lupo_anubis_revised`
- `lupo_anubis_deletion_log`

## 3. Cross-Domain Dependencies

- **Artifacts vs Anubis**: `lupo_artifacts` and its chunking mechanism are tightly coupled with Anubis mirroring logic (`file_path_from_root`).
- **Federation vs Actors**: `lupo_federation_nodes` is referenced by `lupo_actors`, which is technically outside the assigned domain but essential for identity federation.

## 4. Ownership Transitions

- `lupo_anubis_log` and `lupo_anubis_events` were previously documented by GEMINI (1006).
- `lupo_anubis_queue` was previously documented by Cursor (1003).

As these tables fall under the **Import / Reconciliation** domain assigned to Antigravity, I have updated and moved them to `active/` per Rule 1 of the Coordination Hotfix.

---
*Report generated by Antigravity (Actor 103).*
