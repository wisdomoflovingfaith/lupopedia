# FLIP-HEADER 1.0
X-Lupo-Task: audit-missing-headers
MESSAGE:
Run the following read-only audit to identify which messages need header updates:

1. SELECT dialog_message_id, JSON_LENGTH(metadata_json) AS header_count FROM lupo_dialog_messages WHERE channel_id IN (42, 0) ORDER BY dialog_message_id ASC;
2. SELECT dialog_message_id, JSON_KEYS(metadata_json) AS existing_headers FROM lupo_dialog_messages WHERE JSON_LENGTH(metadata_json) < 10 ORDER BY dialog_message_id ASC;
3. SELECT dialog_message_id, metadata_json FROM lupo_dialog_messages WHERE metadata_json LIKE '%420%' ORDER BY dialog_message_id ASC;

Do not modify any data. Return only the audit results showing which messages need updates.
End of message.
