---
lupopedia.headers:
  header_format_version: "4.1.8"
  path_from_lupopedia_root: docs/prd/decisions/pseudocode/08_actors_constitutionpseudo.md
  web_path: https://www.lupopedia.com/lupopedia/docs/prd/decisions/pseudocode/08_actors_constitutionpseudo.md
  status: ''
  when_updated: '20260513033046'
  trust_tier: null
  questions_toon: null
  memory_toon: null
  atoms_toon: null
  transcript_jsonl: null
  artifact_type: documentation
  artifact_kind: pseudocode
  channel_key: null
  federation_node_id: 0
  thread_key: pseudocode-08-actors
  lupopedia.schema: documentation
  prd_cluster: null
  title: ''
  summary: ''
---
# PRD 8: PRD: Actor Identity, Inheritance, and Personalization â€” Shorthand

## Canonical clarification: what a channel is (and is not)

In Lupopedia, a channel is a semantic container inside a domain (node).
It is NOT a Discord room, Slack channel, chat feed, or conversation thread.

**Hierarchy**

```text
Domain (node)
  -> Channel (semantic container)
      -> Thread (artifact)
          -> Messages, memory, atoms, PRDs, and other scoped artifacts
```

**Definition**

- **Channel** = a governed semantic container that defines scope, meaning, and memory boundaries.
- **Thread** = a single conversational artifact inside a channel.
- **Threads do not contain channels. Channels contain threads.**

**Required warning (normative where channels are defined):** Channels are semantic containers, not conversational rooms. They define scope, governance, and meaning for all threads within them.

**Schema keys:** `channel_key`, `channel_id`, and related channel metadata keep their existing names; this section clarifies semantics only (no field renames).

## One-Line Summary

Actor identity, inheritance, and personalization model

## Core Rules

- Only one `auth_user` may extend (lease/control) a given actor at any time.
- The lease is exclusive: no concurrent control or impersonation is permitted.
- Lease state is tracked in the actor's metadata and enforced at the orchestration layer.
- **Core actors** (actor_id < 2026): Must live in `actors/<actor_id>/`
- **Runtime actors** (actor_id >= 20260101010101): Must live in `actors/YYYY/MM/<actor_id>/`
- The registry must always reference numeric actor_id folders
- Existing slug-named folders must be migrated to numeric paths
- Symbolic links may be provided for backward compatibility but must not be used in registry
- The actor registry (`database/lupopedia/actors/actor_id/registry.json`) must enforce canonical numeric folder paths
- All actor workspace references must use the deterministic path based on actor_id

## Forbidden Patterns

- âŒ NO concurrent
- âŒ NO longer
- âŒ NO new

## Required Patterns

- âœ… MUST live in `actors/<actor_id>/`
- âœ… MUST live in `actors/YYYY/MM/<actor_id>/`
- âœ… MUST always reference numeric actor_id folders
- âœ… MUST be migrated to numeric paths
- âœ… MUST not be used in registry

## Constitutional Cross-References

- See PRD 00 for root rules
- See PRD 05 for auth/actor transformation
- See PRD 15 for actor lifecycle
- See PRD 25 for departments
- See PRD 38 for memory unification

## Token-Efficient Checklist

- [ ] Read full PRD for complete context
- [ ] Apply core rules above
- [ ] Check forbidden patterns
- [ ] Verify required patterns
- [ ] Cross-reference with related PRDs

---
*Auto-generated by `scripts/generate_prd_shorthands.py`*
*Source: `docs/prd/08_actors.md`*
*Last sync: 20260408013734*
