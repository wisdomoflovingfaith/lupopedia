# FLARE Header (aliases: Wolfie, FLIP, FLP, FLPH, CROP) see http://www.lupopedia.com/lupopedia/content/FLARE and see http://www.lupopedia.com/lupopedia/qa/FLARE
---
flare.headers:
  file_path_from_root: "docs/database/lupopedia/tables/livehelp_users.md"
  system_version: "4.0.48"
  channel_id: 1
  actor_id: 1007
  last_modified_utc: "20260227"
  delegation_chain: "1007:10000"
  artifact_type: "table_documentation"
  purpose: "Complete documentation for livehelp_users table"
  dialog_message: "Recommended next step: review auto-generated table docs for accuracy and enrich relationships as needed."
  mood_rgb: "4B0082"
  artifact_kind: "table"
  traits: ["canonical", "database", "autogenerated"]
  tags: ["database", "table", "livehelp_users"]
  lupo_agent: "codex-ide"
  livehelp_users.user_id: "int NOT NULL auto_increment"
  livehelp_users.livehelp_id: "bigint unsigned NOT NULL DEFAULT 1"
  livehelp_users.lastaction: "bigint DEFAULT 0"
  livehelp_users.username: "varchar(30) NOT NULL DEFAULT ''"
  livehelp_users.displayname: "varchar(42) NOT NULL DEFAULT ''"
  livehelp_users.password: "varchar(255) COMMENT 'Password hash (nullable for OAuth users)'"
  livehelp_users.timezone_offset: "decimal(4,2) NOT NULL DEFAULT 0.00 COMMENT 'Offset in hours from UTC (can be decimal, e.g., 5.75 for NPT, -3.5 for NST)'"
  livehelp_users.auth_provider: "varchar(20) COMMENT 'OAuth provider name if using OAuth'"
  livehelp_users.provider_id: "varchar(255) COMMENT 'OAuth provider user ID'"
  livehelp_users.isonline: "char(1) NOT NULL DEFAULT ''"
  livehelp_users.isoperator: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.onchannel: "int NOT NULL DEFAULT 0"
  livehelp_users.isadmin: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.department: "int NOT NULL DEFAULT 0"
  livehelp_users.identity: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.status: "varchar(30) NOT NULL DEFAULT ''"
  livehelp_users.isnamed: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.showedup: "bigint"
  livehelp_users.email: "varchar(60) NOT NULL DEFAULT ''"
  livehelp_users.email_verified: "tinyint(1) NOT NULL DEFAULT 0 COMMENT 'Whether email is verified'"
  livehelp_users.verification_token: "varchar(64) COMMENT 'For email verification'"
  livehelp_users.verification_token_expires: "bigint COMMENT 'Expiration timestamp for verification token'"
  livehelp_users.password_reset_token: "varchar(64) COMMENT 'For password reset'"
  livehelp_users.password_reset_expires: "bigint COMMENT 'Expiration timestamp for password reset'"
  livehelp_users.login_token: "varchar(64) COMMENT 'For passwordless login'"
  livehelp_users.login_token_expires: "bigint COMMENT 'Expiration timestamp for login token'"
  livehelp_users.last_login_at: "bigint COMMENT 'Last login timestamp'"
  livehelp_users.last_login_ip: "varchar(45) COMMENT 'IP address of last login'"
  livehelp_users.login_count: "int NOT NULL DEFAULT 0 COMMENT 'Total number of logins'"
  livehelp_users.camefrom: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.show_arrival: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.user_alert: "char(1) NOT NULL DEFAULT 'A'"
  livehelp_users.auto_invite: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.istyping: "char(1) NOT NULL DEFAULT '3'"
  livehelp_users.visits: "int NOT NULL DEFAULT 0"
  livehelp_users.jsrn: "int NOT NULL DEFAULT 0"
  livehelp_users.hostname: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.useragent: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.ipaddress: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.sessionid: "varchar(40) NOT NULL DEFAULT ''"
  livehelp_users.typing_alert: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.authenticated: "char(1) NOT NULL DEFAULT ''"
  livehelp_users.cookied: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.sessiondata: "mediumtext"
  livehelp_users.expires: "bigint NOT NULL DEFAULT 0"
  livehelp_users.greeting: "mediumtext"
  livehelp_users.photo: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.chataction: "bigint DEFAULT 0"
  livehelp_users.new_session: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_users.showtype: "int NOT NULL DEFAULT 1"
  livehelp_users.chattype: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_users.externalchats: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.layerinvite: "int NOT NULL DEFAULT 0"
  livehelp_users.askquestions: "char(1) NOT NULL DEFAULT 'Y'"
  livehelp_users.showvisitors: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.cookieid: "varchar(40) NOT NULL DEFAULT ''"
  livehelp_users.cellphone: "varchar(255) NOT NULL DEFAULT ''"
  livehelp_users.lastcalled: "bigint NOT NULL DEFAULT 0"
  livehelp_users.ismobile: "char(1) DEFAULT 'N'"
  livehelp_users.cell_invite: "char(1) DEFAULT 'N'"
  livehelp_users.useimage: "char(1) NOT NULL DEFAULT 'N'"
  livehelp_users.firstdepartment: "int NOT NULL DEFAULT 0"
  livehelp_users.alertchat: "varchar(45) NOT NULL DEFAULT ''"
  livehelp_users.alerttyping: "varchar(45) NOT NULL DEFAULT ''"
  livehelp_users.alertinsite: "varchar(45) NOT NULL DEFAULT ''"
  table_primary_key: "user_id"
  table_engine: "unknown"
  table_charset: "unknown"
  table_collation: "unknown"
  table_indexes: ["expires","idx_auth_provider","idx_email","idx_login_token","idx_password_reset_token","idx_verification_token","livehelp_id","sessionid"]
  table_foreign_keys: []

# FLARE Edge Automation Tip:
# Use the FLARE Edge Suggester Tool to automatically discover and suggest edges:
# python scripts/flare_edge_suggester.py --file <path> --include-db --format yaml

flare.footer:
  outbound_edges:
    - { to: "docs/toons/livehelp_users.toon.json", type: "schema_reference", weight: 1.0, reason: "TOON schema definition", db_source: "livehelp_users" }
    - { to: "docs/FLARE_HEADERS_COMPLETE_REFERENCE.md", type: "references", weight: 0.9, reason: "FLARE header reference" }
    - { to: "docs/doctrine/FLARE/FLARE_DOCTRINE.md", type: "references", weight: 0.9, reason: "FLARE doctrine" }
    - { to: "docs/doctrine/database/README.md", type: "references", weight: 0.8, reason: "Database doctrine" }
    - { to: "docs/database/lupopedia/README.md", type: "references", weight: 0.8, reason: "Database documentation index" }
  inbound_edges: []
  semantic_tags: ["database", "table", "livehelp_users"]
  version: "4.0.48"
  last_verified: "20260227"
  last_verified_by: "codex-ide"
---

# Table: livehelp_users

Purpose: Auto-generated documentation for livehelp_users from TOON schema.
Type: database_table
Status: production_ready
Volume: unknown

## 1. Overview
- Key responsibilities: schema reference, storage, and lookup for this table.
- System role: persists data for the Lupopedia database subsystem.
- Importance: enables data integrity and downstream features tied to this table.

## 2. Schema Reference
Primary Key: user_id
Field Categories: see full field list below.

### All Fields
| Column | Type | Notes |
|---|---|---|
| user_id | int NOT NULL auto_increment | from TOON |
| livehelp_id | bigint unsigned NOT NULL DEFAULT 1 | from TOON |
| lastaction | bigint DEFAULT 0 | from TOON |
| username | varchar(30) NOT NULL DEFAULT '' | from TOON |
| displayname | varchar(42) NOT NULL DEFAULT '' | from TOON |
| password | varchar(255) COMMENT 'Password hash (nullable for OAuth users)' | from TOON |
| timezone_offset | decimal(4,2) NOT NULL DEFAULT 0.00 COMMENT 'Offset in hours from UTC (can be decimal, e.g., 5.75 for NPT, -3.5 for NST)' | from TOON |
| auth_provider | varchar(20) COMMENT 'OAuth provider name if using OAuth' | from TOON |
| provider_id | varchar(255) COMMENT 'OAuth provider user ID' | from TOON |
| isonline | char(1) NOT NULL DEFAULT '' | from TOON |
| isoperator | char(1) NOT NULL DEFAULT 'N' | from TOON |
| onchannel | int NOT NULL DEFAULT 0 | from TOON |
| isadmin | char(1) NOT NULL DEFAULT 'N' | from TOON |
| department | int NOT NULL DEFAULT 0 | from TOON |
| identity | varchar(255) NOT NULL DEFAULT '' | from TOON |
| status | varchar(30) NOT NULL DEFAULT '' | from TOON |
| isnamed | char(1) NOT NULL DEFAULT 'N' | from TOON |
| showedup | bigint | from TOON |
| email | varchar(60) NOT NULL DEFAULT '' | from TOON |
| email_verified | tinyint(1) NOT NULL DEFAULT 0 COMMENT 'Whether email is verified' | from TOON |
| verification_token | varchar(64) COMMENT 'For email verification' | from TOON |
| verification_token_expires | bigint COMMENT 'Expiration timestamp for verification token' | from TOON |
| password_reset_token | varchar(64) COMMENT 'For password reset' | from TOON |
| password_reset_expires | bigint COMMENT 'Expiration timestamp for password reset' | from TOON |
| login_token | varchar(64) COMMENT 'For passwordless login' | from TOON |
| login_token_expires | bigint COMMENT 'Expiration timestamp for login token' | from TOON |
| last_login_at | bigint COMMENT 'Last login timestamp' | from TOON |
| last_login_ip | varchar(45) COMMENT 'IP address of last login' | from TOON |
| login_count | int NOT NULL DEFAULT 0 COMMENT 'Total number of logins' | from TOON |
| camefrom | varchar(255) NOT NULL DEFAULT '' | from TOON |
| show_arrival | char(1) NOT NULL DEFAULT 'N' | from TOON |
| user_alert | char(1) NOT NULL DEFAULT 'A' | from TOON |
| auto_invite | char(1) NOT NULL DEFAULT 'N' | from TOON |
| istyping | char(1) NOT NULL DEFAULT '3' | from TOON |
| visits | int NOT NULL DEFAULT 0 | from TOON |
| jsrn | int NOT NULL DEFAULT 0 | from TOON |
| hostname | varchar(255) NOT NULL DEFAULT '' | from TOON |
| useragent | varchar(255) NOT NULL DEFAULT '' | from TOON |
| ipaddress | varchar(255) NOT NULL DEFAULT '' | from TOON |
| sessionid | varchar(40) NOT NULL DEFAULT '' | from TOON |
| typing_alert | char(1) NOT NULL DEFAULT 'N' | from TOON |
| authenticated | char(1) NOT NULL DEFAULT '' | from TOON |
| cookied | char(1) NOT NULL DEFAULT 'N' | from TOON |
| sessiondata | mediumtext | from TOON |
| expires | bigint NOT NULL DEFAULT 0 | from TOON |
| greeting | mediumtext | from TOON |
| photo | varchar(255) NOT NULL DEFAULT '' | from TOON |
| chataction | bigint DEFAULT 0 | from TOON |
| new_session | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| showtype | int NOT NULL DEFAULT 1 | from TOON |
| chattype | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| externalchats | varchar(255) NOT NULL DEFAULT '' | from TOON |
| layerinvite | int NOT NULL DEFAULT 0 | from TOON |
| askquestions | char(1) NOT NULL DEFAULT 'Y' | from TOON |
| showvisitors | char(1) NOT NULL DEFAULT 'N' | from TOON |
| cookieid | varchar(40) NOT NULL DEFAULT '' | from TOON |
| cellphone | varchar(255) NOT NULL DEFAULT '' | from TOON |
| lastcalled | bigint NOT NULL DEFAULT 0 | from TOON |
| ismobile | char(1) DEFAULT 'N' | from TOON |
| cell_invite | char(1) DEFAULT 'N' | from TOON |
| useimage | char(1) NOT NULL DEFAULT 'N' | from TOON |
| firstdepartment | int NOT NULL DEFAULT 0 | from TOON |
| alertchat | varchar(45) NOT NULL DEFAULT '' | from TOON |
| alerttyping | varchar(45) NOT NULL DEFAULT '' | from TOON |
| alertinsite | varchar(45) NOT NULL DEFAULT '' | from TOON |

## 3. Relationships and Dependencies
- Primary relationships: not specified in TOON relationships array.
- Referencing tables: unknown (use edge suggester tool).
- Integration points: see outbound edges in FLARE footer.

## 4. Indexes and Performance
Primary Indexes:
- user_id
Performance Indexes:
- expires
- idx_auth_provider
- idx_email
- idx_login_token
- idx_password_reset_token
- idx_verification_token
- livehelp_id
- sessionid
Index Strategy: derived from TOON index definitions.

## 5. Usage Patterns
Common Queries:
```sql
SELECT * FROM livehelp_users WHERE user_id = :id;
SELECT COUNT(*) AS total FROM livehelp_users WHERE is_deleted = 0;
SELECT * FROM livehelp_users ORDER BY user_id DESC LIMIT 25;
UPDATE livehelp_users SET updated_ymdhis = :ts WHERE user_id = :id;
```
Best Practices: always filter soft deletes where applicable.
Anti-Patterns: avoid full table scans on large datasets.

## 6. Performance Considerations
- High-volume operations: dependent on feature usage.
- Optimization tips: rely on existing indexes; add new indexes only with TOON updates.
- Scaling considerations: paginate reads and batch writes.

## 7. Data Integrity
- Constraints: see NOT NULL and DEFAULT values in TOON fields.
- Validation rules: enforced at application layer.
- Soft delete: use is_deleted/deleted_ymdhis if present.

## 8. Common Issues and Solutions
- Performance issues: add missing indexes via schema update.
- Data consistency: ensure foreign key relationships are enforced in application logic.
- Troubleshooting: compare against TOON schema for mismatches.

## 9. Future Enhancements
- Enrich relationships with discovered edges.
- Add usage-specific examples once feature usage is known.
