# FLARE Header (aliases: Wolfie, FLIP, FLP, FLPH, CROP) see http://www.lupopedia.com/lupopedia/content/FLARE and see http://www.lupopedia.com/lupopedia/qa/FLARE
---
flare.headers:
  file_path_from_root: "docs/database/lupopedia/tables/lupo_federation_discovery.md"
  system_version: "4.0.48"
  channel_id: 1
  actor_id: 1007
  last_modified_utc: "20260227"
  delegation_chain: "1007:10000"
  artifact_type: "table_documentation"
  purpose: "Complete documentation for lupo_federation_discovery table"
  dialog_message: "Recommended next step: review auto-generated table docs for accuracy and enrich relationships as needed."
  mood_rgb: "4B0082"
  artifact_kind: "table"
  traits: ["canonical", "database", "autogenerated"]
  tags: ["database", "table", "lupo_federation_discovery"]
  lupo_agent: "codex-ide"
  lupo_federation_discovery.federation_discovery_id: "bigint NOT NULL"
  lupo_federation_discovery.domain: "varchar(255) NOT NULL"
  lupo_federation_discovery.install_url: "varchar(500)"
  lupo_federation_discovery.is_lupopedia: "tinyint NOT NULL DEFAULT 0"
  lupo_federation_discovery.last_seen_ymdhis: "bigint"
  lupo_federation_discovery.first_seen_ymdhis: "bigint"
  lupo_federation_discovery.hashtag_count: "bigint DEFAULT 0"
  lupo_federation_discovery.question_count: "bigint DEFAULT 0"
  lupo_federation_discovery.atom_count: "bigint DEFAULT 0"
  lupo_federation_discovery.context_count: "bigint DEFAULT 0"
  lupo_federation_discovery.collection_count: "bigint DEFAULT 0"
  lupo_federation_discovery.keywords: "varchar(500)"
  lupo_federation_discovery.description: "text"
  lupo_federation_discovery.import_hashtags: "tinyint NOT NULL DEFAULT 0"
  lupo_federation_discovery.import_questions: "tinyint NOT NULL DEFAULT 0"
  lupo_federation_discovery.import_atoms: "tinyint NOT NULL DEFAULT 0"
  lupo_federation_discovery.import_contexts: "tinyint NOT NULL DEFAULT 0"
  lupo_federation_discovery.import_collections: "tinyint NOT NULL DEFAULT 0"
  lupo_federation_discovery.created_ymdhis: "bigint NOT NULL DEFAULT 0"
  lupo_federation_discovery.updated_ymdhis: "bigint NOT NULL"
  table_primary_key: "federation_discovery_id"
  table_engine: "unknown"
  table_charset: "unknown"
  table_collation: "unknown"
  table_indexes: "lupo_federation_discovery_idx_domain"
  table_foreign_keys: []

# FLARE Edge Automation Tip:
# Use the FLARE Edge Suggester Tool to automatically discover and suggest edges:
# python scripts/flare_edge_suggester.py --file <path> --include-db --format yaml

flare.footer:
  outbound_edges:
    - { to: "docs/toons/lupo_federation_discovery.toon.json", type: "schema_reference", weight: 1.0, reason: "TOON schema definition", db_source: "lupo_federation_discovery" }
    - { to: "docs/FLARE_HEADERS_COMPLETE_REFERENCE.md", type: "references", weight: 0.9, reason: "FLARE header reference" }
    - { to: "docs/doctrine/FLARE/FLARE_DOCTRINE.md", type: "references", weight: 0.9, reason: "FLARE doctrine" }
    - { to: "docs/doctrine/database/README.md", type: "references", weight: 0.8, reason: "Database doctrine" }
    - { to: "docs/database/lupopedia/README.md", type: "references", weight: 0.8, reason: "Database documentation index" }
  inbound_edges: []
  semantic_tags: ["database", "table", "lupo_federation_discovery"]
  version: "4.0.48"
  last_verified: "20260227"
  last_verified_by: "codex-ide"
---

# Table: lupo_federation_discovery

Purpose: Auto-generated documentation for lupo_federation_discovery from TOON schema.
Type: database_table
Status: production_ready
Volume: unknown

## 1. Overview
- Key responsibilities: schema reference, storage, and lookup for this table.
- System role: persists data for the Lupopedia database subsystem.
- Importance: enables data integrity and downstream features tied to this table.

## 2. Schema Reference
Primary Key: federation_discovery_id
Field Categories: see full field list below.

### All Fields
| Column | Type | Notes |
|---|---|---|
| federation_discovery_id | bigint NOT NULL | from TOON |
| domain | varchar(255) NOT NULL | from TOON |
| install_url | varchar(500) | from TOON |
| is_lupopedia | tinyint NOT NULL DEFAULT 0 | from TOON |
| last_seen_ymdhis | bigint | from TOON |
| first_seen_ymdhis | bigint | from TOON |
| hashtag_count | bigint DEFAULT 0 | from TOON |
| question_count | bigint DEFAULT 0 | from TOON |
| atom_count | bigint DEFAULT 0 | from TOON |
| context_count | bigint DEFAULT 0 | from TOON |
| collection_count | bigint DEFAULT 0 | from TOON |
| keywords | varchar(500) | from TOON |
| description | text | from TOON |
| import_hashtags | tinyint NOT NULL DEFAULT 0 | from TOON |
| import_questions | tinyint NOT NULL DEFAULT 0 | from TOON |
| import_atoms | tinyint NOT NULL DEFAULT 0 | from TOON |
| import_contexts | tinyint NOT NULL DEFAULT 0 | from TOON |
| import_collections | tinyint NOT NULL DEFAULT 0 | from TOON |
| created_ymdhis | bigint NOT NULL DEFAULT 0 | from TOON |
| updated_ymdhis | bigint NOT NULL | from TOON |

## 3. Relationships and Dependencies
- Primary relationships: not specified in TOON relationships array.
- Referencing tables: unknown (use edge suggester tool).
- Integration points: see outbound edges in FLARE footer.

## 4. Indexes and Performance
Primary Indexes:
- federation_discovery_id
Performance Indexes:
- lupo_federation_discovery_idx_domain
Index Strategy: derived from TOON index definitions.

## 5. Usage Patterns
Common Queries:
```sql
SELECT * FROM lupo_federation_discovery WHERE federation_discovery_id = :id;
SELECT COUNT(*) AS total FROM lupo_federation_discovery WHERE is_deleted = 0;
SELECT * FROM lupo_federation_discovery ORDER BY federation_discovery_id DESC LIMIT 25;
UPDATE lupo_federation_discovery SET updated_ymdhis = :ts WHERE federation_discovery_id = :id;
```
Best Practices: always filter soft deletes where applicable.
Anti-Patterns: avoid full table scans on large datasets.

## 6. Performance Considerations
- High-volume operations: dependent on feature usage.
- Optimization tips: rely on existing indexes; add new indexes only with TOON updates.
- Scaling considerations: paginate reads and batch writes.

## 7. Data Integrity
- Constraints: see NOT NULL and DEFAULT values in TOON fields.
- Validation rules: enforced at application layer.
- Soft delete: use is_deleted/deleted_ymdhis if present.

## 8. Common Issues and Solutions
- Performance issues: add missing indexes via schema update.
- Data consistency: ensure foreign key relationships are enforced in application logic.
- Troubleshooting: compare against TOON schema for mismatches.

## 9. Future Enhancements
- Enrich relationships with discovered edges.
- Add usage-specific examples once feature usage is known.
