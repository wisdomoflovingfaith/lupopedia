# FLARE Header (aliases: Wolfie, FLIP, FLP, FLPH, CROP) see http://www.lupopedia.com/lupopedia/content/FLARE and see http://www.lupopedia.com/lupopedia/qa/FLARE
---
flare.headers:
  file_path_from_root: "docs/database/lupopedia/tables/lupo_sessions.md"
  system_version: "4.0.48"
  channel_id: 1
  actor_id: 1007
  last_modified_utc: "20260227"
  delegation_chain: "1007:10000"
  artifact_type: "table_documentation"
  purpose: "Complete documentation for lupo_sessions table"
  dialog_message: "Recommended next step: review auto-generated table docs for accuracy and enrich relationships as needed."
  mood_rgb: "4B0082"
  artifact_kind: "table"
  traits: ["canonical", "database", "autogenerated"]
  tags: ["database", "table", "lupo_sessions"]
  lupo_agent: "codex-ide"
  lupo_sessions.session_id: "varchar(255) NOT NULL"
  lupo_sessions.federation_node_id: "bigint NOT NULL DEFAULT 1"
  lupo_sessions.actor_id: "bigint NOT NULL DEFAULT 0"
  lupo_sessions.channel_id: "bigint NOT NULL DEFAULT 1"
  lupo_sessions.ip_address: "varchar(45) NOT NULL DEFAULT ''"
  lupo_sessions.user_agent: "varchar(255) NOT NULL DEFAULT ''"
  lupo_sessions.device_id: "varchar(100)"
  lupo_sessions.device_type: "varchar(64)"
  lupo_sessions.auth_method: "varchar(30)"
  lupo_sessions.auth_provider: "varchar(50)"
  lupo_sessions.security_level: "varchar(64) NOT NULL DEFAULT 'medium'"
  lupo_sessions.name_key: "varchar(100)"
  lupo_sessions.is_named: "tinyint NOT NULL DEFAULT 0"
  lupo_sessions.is_authenticated: "tinyint NOT NULL DEFAULT 0"
  lupo_sessions.is_active: "tinyint NOT NULL DEFAULT 1"
  lupo_sessions.is_expired: "tinyint NOT NULL DEFAULT 0"
  lupo_sessions.is_revoked: "tinyint NOT NULL DEFAULT 0"
  lupo_sessions.session_data: "text"
  lupo_sessions.system_context: "varchar(50)"
  lupo_sessions.metadata: "json"
  lupo_sessions.login_ymdhis: "bigint"
  lupo_sessions.last_seen_ymdhis: "bigint NOT NULL"
  lupo_sessions.expires_ymdhis: "bigint"
  lupo_sessions.created_ymdhis: "bigint NOT NULL DEFAULT 0"
  lupo_sessions.updated_ymdhis: "bigint NOT NULL"
  lupo_sessions.is_deleted: "tinyint NOT NULL DEFAULT 0"
  lupo_sessions.deleted_ymdhis: "bigint"
  table_primary_key: "session_id"
  table_engine: "unknown"
  table_charset: "unknown"
  table_collation: "unknown"
  table_indexes: ["lupo_sessions_idx_actor","lupo_sessions_idx_cleanup","lupo_sessions_idx_created","lupo_sessions_idx_device","lupo_sessions_idx_domain","lupo_sessions_idx_expires","lupo_sessions_idx_last_seen","lupo_sessions_idx_security","lupo_sessions_idx_status"]
  table_foreign_keys: []

# FLARE Edge Automation Tip:
# Use the FLARE Edge Suggester Tool to automatically discover and suggest edges:
# python scripts/flare_edge_suggester.py --file <path> --include-db --format yaml

flare.footer:
  outbound_edges:
    - { to: "docs/toons/lupo_sessions.toon.json", type: "schema_reference", weight: 1.0, reason: "TOON schema definition", db_source: "lupo_sessions" }
    - { to: "docs/FLARE_HEADERS_COMPLETE_REFERENCE.md", type: "references", weight: 0.9, reason: "FLARE header reference" }
    - { to: "docs/doctrine/FLARE/FLARE_DOCTRINE.md", type: "references", weight: 0.9, reason: "FLARE doctrine" }
    - { to: "docs/doctrine/database/README.md", type: "references", weight: 0.8, reason: "Database doctrine" }
    - { to: "docs/database/lupopedia/README.md", type: "references", weight: 0.8, reason: "Database documentation index" }
  inbound_edges: []
  semantic_tags: ["database", "table", "lupo_sessions"]
  version: "4.0.48"
  last_verified: "20260227"
  last_verified_by: "codex-ide"
---

# Table: lupo_sessions

Purpose: Auto-generated documentation for lupo_sessions from TOON schema.
Type: database_table
Status: production_ready
Volume: unknown

## 1. Overview
- Key responsibilities: schema reference, storage, and lookup for this table.
- System role: persists data for the Lupopedia database subsystem.
- Importance: enables data integrity and downstream features tied to this table.

## 2. Schema Reference
Primary Key: session_id
Field Categories: see full field list below.

### All Fields
| Column | Type | Notes |
|---|---|---|
| session_id | varchar(255) NOT NULL | from TOON |
| federation_node_id | bigint NOT NULL DEFAULT 1 | from TOON |
| actor_id | bigint NOT NULL DEFAULT 0 | from TOON |
| channel_id | bigint NOT NULL DEFAULT 1 | from TOON |
| ip_address | varchar(45) NOT NULL DEFAULT '' | from TOON |
| user_agent | varchar(255) NOT NULL DEFAULT '' | from TOON |
| device_id | varchar(100) | from TOON |
| device_type | varchar(64) | from TOON |
| auth_method | varchar(30) | from TOON |
| auth_provider | varchar(50) | from TOON |
| security_level | varchar(64) NOT NULL DEFAULT 'medium' | from TOON |
| name_key | varchar(100) | from TOON |
| is_named | tinyint NOT NULL DEFAULT 0 | from TOON |
| is_authenticated | tinyint NOT NULL DEFAULT 0 | from TOON |
| is_active | tinyint NOT NULL DEFAULT 1 | from TOON |
| is_expired | tinyint NOT NULL DEFAULT 0 | from TOON |
| is_revoked | tinyint NOT NULL DEFAULT 0 | from TOON |
| session_data | text | from TOON |
| system_context | varchar(50) | from TOON |
| metadata | json | from TOON |
| login_ymdhis | bigint | from TOON |
| last_seen_ymdhis | bigint NOT NULL | from TOON |
| expires_ymdhis | bigint | from TOON |
| created_ymdhis | bigint NOT NULL DEFAULT 0 | from TOON |
| updated_ymdhis | bigint NOT NULL | from TOON |
| is_deleted | tinyint NOT NULL DEFAULT 0 | from TOON |
| deleted_ymdhis | bigint | from TOON |

## 3. Relationships and Dependencies
- Primary relationships: not specified in TOON relationships array.
- Referencing tables: unknown (use edge suggester tool).
- Integration points: see outbound edges in FLARE footer.

## 4. Indexes and Performance
Primary Indexes:
- session_id
Performance Indexes:
- lupo_sessions_idx_actor
- lupo_sessions_idx_cleanup
- lupo_sessions_idx_created
- lupo_sessions_idx_device
- lupo_sessions_idx_domain
- lupo_sessions_idx_expires
- lupo_sessions_idx_last_seen
- lupo_sessions_idx_security
- lupo_sessions_idx_status
Index Strategy: derived from TOON index definitions.

## 5. Usage Patterns
Common Queries:
```sql
SELECT * FROM lupo_sessions WHERE session_id = :id;
SELECT COUNT(*) AS total FROM lupo_sessions WHERE is_deleted = 0;
SELECT * FROM lupo_sessions ORDER BY session_id DESC LIMIT 25;
UPDATE lupo_sessions SET updated_ymdhis = :ts WHERE session_id = :id;
```
Best Practices: always filter soft deletes where applicable.
Anti-Patterns: avoid full table scans on large datasets.

## 6. Performance Considerations
- High-volume operations: dependent on feature usage.
- Optimization tips: rely on existing indexes; add new indexes only with TOON updates.
- Scaling considerations: paginate reads and batch writes.

## 7. Data Integrity
- Constraints: see NOT NULL and DEFAULT values in TOON fields.
- Validation rules: enforced at application layer.
- Soft delete: use is_deleted/deleted_ymdhis if present.

## 8. Common Issues and Solutions
- Performance issues: add missing indexes via schema update.
- Data consistency: ensure foreign key relationships are enforced in application logic.
- Troubleshooting: compare against TOON schema for mismatches.

## 9. Future Enhancements
- Enrich relationships with discovered edges.
- Add usage-specific examples once feature usage is known.
